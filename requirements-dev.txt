black
ipykernel
pytest
ruff
# Scraping ulasan implementasi OmorfoShop (otomasi browser, tanpa API).
# Setelah install: `python -m playwright install chromium`
playwright>=1.40
# Generasi PDF dokumentasi Fase 10 (scripts/md_to_pdf.py) — pure-Python.
markdown>=3.6
xhtml2pdf>=0.2.16
